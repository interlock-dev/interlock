{
  "name": "interlock-action",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node20 --format=cjs --outfile=dist/index.cjs"
  },
  "dependencies": {
    "@actions/core": "^3.0.0",
    "@actions/github": "^6.0.0",
    "interlock-core": "0.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
